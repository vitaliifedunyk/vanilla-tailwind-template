# vanilla-tailwind-template

## 🏷 Назва проєкту
`vanilla-tailwind-template`

## 📄 Опис
Це стартовий шаблон на `Vanilla JavaScript` + `TailwindCSS` (через Vite) для швидкого запуску нових фронтенд-проєктів. Шаблон містить базову структуру, підключений Tailwind і готовий dev/build цикл.

## 🚀 Початок роботи
1. Клонуйте репозиторій:
```bash
git clone https://github.com/<your-username>/vanilla-tailwind-template.git
cd vanilla-tailwind-template
```
2. Встановіть залежності:
```bash
npm install
```
3. Запустіть dev-сервер:
```bash
npm run dev
```

Після запуску відкрийте адресу, яку покаже Vite (зазвичай `http://localhost:5173`).

## 🛠 Технологічний стек
- `HTML5`
- `TailwindCSS` (`tailwindcss`, `@tailwindcss/vite`)
- `Vanilla JavaScript` (ES Modules)
- `Vite` як інструмент збірки та локальної розробки
- Плагін `vite-plugin-html-inject` для інжекції HTML у Vite-конфігурації

## 📁 Структура проєкту
```text
.
├── index.html
├── src/
│   ├── main.js
│   └── css/
│       └── style.css
├── vite.config.js
├── package.json
└── .github/workflows/deploy.yml
```

## ⚙️ Скрипти
- `npm run dev` — запускає локальний сервер розробки через Vite.
- `npm run build` — створює production-збірку в `dist/`.
- `npm run preview` — локально переглядає зібраний проєкт.

## 👤 Автор
**Студент курсу GoIT**  
GitHub: https://github.com/vitaliifedunyk
